# Development environment settings for GitHub Codespaces
# Disables WebSocket for hot reload (fixes connection errors in Codespaces)
WDS_SOCKET_PORT=0
FAST_REFRESH=false

# Use polling for file changes (more reliable in containerized environments)
CHOKIDAR_USEPOLLING=true

# Suppress source map warnings from zxing library
GENERATE_SOURCEMAP=false
