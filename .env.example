# NutriNote+ Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# API KEY (Required for AI Nutrition Feature)
# ===========================================

# OpenRouter API Key for AI nutrition estimation
# Get yours from: https://openrouter.ai/keys
# Note: This is used server-side only (proxy-server.js and Vercel serverless function)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ===========================================
# USDA FOODDATA CENTRAL (Recommended)
# ===========================================

# Free API key for validated USDA nutrition data.
# Sign up at: https://fdc.nal.usda.gov/api-key-signup.html
# Without a key the USDA API still works but shares a 1000 req/hour anonymous quota.
# This key is SERVER-SIDE ONLY (no REACT_APP_ prefix) — never sent to the browser.
USDA_API_KEY=your_usda_fdcapi_key_here

# ===========================================
# VERCEL DEPLOYMENT
# ===========================================
# When deploying to Vercel, add both keys in:
# Project Settings → Environment Variables
#
# OPENROUTER_API_KEY → Production, Preview, Development
# USDA_API_KEY       → Production, Preview, Development
